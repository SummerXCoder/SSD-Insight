spring:
  datasource:
    url: jdbc:mysql://localhost:3306/meeting_room?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root

logging:
  level:
    root: INFO
    com.company.meeting: DEBUG
